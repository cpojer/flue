---
title: Sandbox API
description: Configure isolated execution environments and adapt sandbox connectors.
---

## Sandbox configuration

TODO: Document `SandboxFactory` as the agent sandbox configuration contract.

## `createSandboxSessionEnv(...)`

TODO: Document adapting connector-provided `SandboxApi` implementations into session environments.

## `SandboxApi` and `SessionEnv`

TODO: Reference execution, filesystem, and environment interfaces.

## Shell adapters

TODO: Reference `BashFactory`, `BashLike`, and `FileStat` for virtual shell integrations.

## Platform sandbox integrations

TODO: Point to the Node.js and Cloudflare platform API pages for built-in implementations.
