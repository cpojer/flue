---
title: Harness API
description: Reference initialized harnesses, sessions, prompting operations, and results.
---

## `FlueHarness`

TODO: Document initialized agent environments, named harness scopes, filesystem access, and shell operations.

## `FlueSessions` and `FlueSession`

TODO: Reference session selection, lifecycle methods, and named state boundaries.

## `session.prompt(...)`

TODO: Document prompt inputs, images, model overrides, responses, usage, and structured results.

## `session.skill(...)`, `session.task(...)`, and `session.shell(...)`

TODO: Document operation signatures and associated option/result types.

## Structured results

TODO: Reference `PromptResultResponse<T>` and `ResultUnavailableError`.

## Compaction and deletion

TODO: Document manual compaction and session deletion operations.

## `CallHandle<T>` and `FlueFs`

TODO: Reference operation handles and filesystem methods available on sessions and harnesses.
