---
title: Agent API
description: Define agent profiles and created agents with the core runtime API.
---

## Imports

TODO: Document agent-authoring imports from `@flue/runtime`.

## `defineAgentProfile(...)`

TODO: Document reusable agent profile definitions, defaults, tools, skills, subagents, and model configuration.

## `createAgent(...)`

TODO: Document created agent initializers and the `{ id, env, payload }` creation context.

## Configuration types

TODO: Reference `AgentProfile`, `AgentRuntimeConfig`, `AgentConfig`, `ModelConfig`, `CompactionConfig`, `ThinkingLevel`, and `ProviderSettings`.

## Created agent types

TODO: Reference `CreatedAgent`, `AgentCreateContext`, and `AgentHarnessOptions`.
