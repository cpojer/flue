---
title: client.workflows.connect()
description: Connect over WebSockets to invoke a deployed workflow and receive its run events.
---

## Signature

TODO: Document workflow name selection and returned connection.

## Invoke a workflow

TODO: Reference a workflow socket's single invocation lifecycle and payload input.

## Receive a workflow run ID

TODO: Document when invocation creates and returns `runId`.

## Subscribe to events

TODO: Document workflow socket event listeners and event identity.

## Types

TODO: Reference `WorkflowSocket`, `WorkflowSocketInvokeResult`, and `WorkflowSocketEventContext`.
