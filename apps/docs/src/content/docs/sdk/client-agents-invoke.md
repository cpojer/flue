---
title: client.agents.invoke()
description: Invoke a deployed agent instance synchronously or as a streamed interaction.
---

## Signature

TODO: Document agent name, stable instance `id`, payload, invocation mode, and abort signal.

## Synchronous invocation

TODO: Document `{ mode: 'sync' }` and its terminal result shape.

## Streaming invocation

TODO: Document `{ mode: 'stream' }` and attached-agent event consumption.

## Payload and sessions

TODO: Reference `DirectAgentPayload`, including message and optional session selection.

## Agent interactions are not runs

TODO: State that direct agent invocation is correlated to instance/session identity, not workflow `runId`.

## Types

TODO: Reference `DirectAgentPayload`, `AttachedAgentEvent`, and `AttachedAgentStreamError`.
