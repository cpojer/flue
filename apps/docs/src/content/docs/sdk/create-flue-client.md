---
title: createFlueClient()
description: Create and configure a client for deployed Flue application routes.
---

## Import

TODO: Document importing `createFlueClient` from `@flue/sdk`.

## Signature

TODO: Reference the factory signature and returned `FlueClient` object.

## HTTP options

TODO: Document `baseUrl`, `fetch`, `headers`, and `token` options.

## WebSocket options

TODO: Document custom WebSocket implementations, base paths, and URL transformations.

## Admin route options

TODO: Document `adminBasePath` for optionally mounted read-only routes.

## Types

TODO: Reference `CreateFlueClientOptions`, `FlueClient`, and `RequestHeaders`.
