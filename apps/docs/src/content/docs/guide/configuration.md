---
title: Configuration
description: Configure project targets, source roots, outputs, and development environment behavior.
---

## Create a configuration file

TODO: Introduce `flue.config.ts` and `defineConfig()` as build-oriented configuration.

## Select a runtime target

TODO: Explain selecting Node.js or Cloudflare for build and development behavior.

## Configure source root and output

TODO: Cover `root`, `output`, and relative path resolution.

## Override configuration from the CLI

TODO: Explain precedence between command flags and configuration file values.

## Load environment files locally

TODO: Cover the `--env` development/run workflow and precedence without documenting secrets in authored content.

## Keep runtime configuration elsewhere

TODO: Point model provider setup, application middleware, and observers to `app.ts` and the relevant guides.
